[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning
    # PyJWT complains that the dev JWT_SECRET is under 32 bytes. It is -- see
    # the "issues" note. Production refuses to boot with a short key.
    ignore::UserWarning
